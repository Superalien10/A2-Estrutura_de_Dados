#ifndef FUNCOES_VISUALIZACAO_HPP
#define FUNCOES_VISUALIZACAO_HPP

#include <string>
#include "funcoesLista.hpp"
using namespace std;
void visualizeList(struct ListNode*, struct ListNode*, struct ListNode*);
void setPositions(struct ListNode*, struct ListNode*);
#endif
